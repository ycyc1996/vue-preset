{
  "name": "vue-preset",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create-demo": "rm -rf local && mkdir local && cd local && vue create --preset .. demo"
  },
  "author": "",
  "license": "ISC"
}
